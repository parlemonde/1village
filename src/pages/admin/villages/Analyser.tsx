import React from 'react';

const Analyser = () => {
  const renderTitle = () => {
    return (
      <div>
        <h1>Analyser</h1>
      </div>
    );
  };

  return <>{renderTitle()}</>;
};

export default Analyser;
