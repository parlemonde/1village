import React from 'react';

export const Analyser = () => {
  const renderTitle = () => {
    return (
      <div>
        <h1>Analyser</h1>
      </div>
    );
  };

  return <>{renderTitle()}</>;
};
