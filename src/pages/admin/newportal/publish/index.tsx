import React from 'react';

const Publier = () => {
  const renderTitle = () => {
    return (
      <div>
        <h1>Publier</h1>
      </div>
    );
  };

  return <>{renderTitle()}</>;
};

export default Publier;
