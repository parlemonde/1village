import React from 'react';

const Mediatheque = () => {
  const renderTitle = () => {
    return (
      <div>
        <h1>Mediatheque</h1>
      </div>
    );
  };

  return <>{renderTitle()}</>;
};

export default Mediatheque;
