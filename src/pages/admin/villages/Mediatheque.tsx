import React from 'react';

export const Mediatheque = () => {
  const renderTitle = () => {
    return (
      <div>
        <h1>Mediatheque</h1>
      </div>
    );
  };

  return <>{renderTitle()}</>;
};
