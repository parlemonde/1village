import React from 'react';

const Gerer = () => {
  const renderTitle = () => {
    return (
      <div>
        <h1>Gérer</h1>
      </div>
    );
  };

  return <>{renderTitle()}</>;
};

export default Gerer;
